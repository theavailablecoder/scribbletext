services:
  - type: web
    name: scribbletext-api
    runtime: node
    buildCommand: npm install
    startCommand: node server/server.js
    envVars:
      - key: TURSO_DATABASE_URL
        sync: false
      - key: TURSO_AUTH_TOKEN
        sync: false
      - key: NODE_VERSION
        value: 20
